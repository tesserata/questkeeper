[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "questkeeper-domain"
version = "0.1.0"
description = "Domain service"
requires-python = ">=3.13"
dependencies = ["grpcio==1.76.0", "grpcio-tools==1.76.0", "protobuf==6.33.0"]
