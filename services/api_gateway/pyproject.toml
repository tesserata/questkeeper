[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "questkeeper-gateway"
version = "0.1.0"
description = "API Gateway"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "questkeeper-render-sdk==0.1.*",
  "questkeeper-api-contracts==0.1.*",
]
